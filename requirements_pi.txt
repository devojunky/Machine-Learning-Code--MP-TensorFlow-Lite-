# --- Core scientific + vision stack (Python 3.11 compatible) ---
numpy==1.26.4
opencv-python==4.11.0.86
mediapipe==0.10.9
joblib==1.5.2
scikit-learn==1.7.2
scipy==1.16.2

# --- Inference backend (XGBoost only, no TensorFlow) ---
xgboost==3.1.1

# --- Visualization / utils ---
matplotlib==3.10.7
pandas==2.3.3

# --- Optional (progress bars / convenience) ---
tqdm==4.67.1

# --- Raspberry Pi Camera specific ---
# Note: Requires system packages to be installed first on Pi OS:
# sudo apt update && sudo apt install -y python3-picamera2 libcamera-dev
picamera2==0.3.12

# --- Bluetooth Audio Receiver (A2DP Sink) ---
# Note: Bluetooth functionality uses subprocess to call system commands.
# The following system packages are required (install via apt, not pip):
#   pulseaudio
#   pulseaudio-module-bluetooth
#   bluez
#   bluez-tools
#   alsa-utils (for amixer volume control)
#
# Install system packages with:
#   sudo apt-get update
#   sudo apt-get install -y pulseaudio pulseaudio-module-bluetooth bluez bluez-tools alsa-utils
#
# Optional: dbus-python for advanced D-Bus integration (not required for basic functionality)
# dbus-python==1.3.2
